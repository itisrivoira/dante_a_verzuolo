const playAudio = () => {
    var audio_player = document.getElementById("audio_player"); 
    // if (audio_player.isPlaying())
    //     audio_player.pause();
    // else
        audio_player.play();
}